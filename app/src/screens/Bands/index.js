import React from 'react';
import {Text} from 'react-native';

import {Container} from './styles';

const Bands = () => {
  return (
    <Container>
      <Text>Bands Screen</Text>
    </Container>
  );
};

export default Bands;
